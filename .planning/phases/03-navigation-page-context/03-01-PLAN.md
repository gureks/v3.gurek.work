---
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/MainLayout.tsx
  - frontend/src/components/layout/DesktopSidebar.tsx
  - frontend/src/components/layout/MobileMenu.tsx
autonomous: true
requirements: LAY-03, LAY-04, LAY-05
---
<plan>
<task>
  <objective>
    Create the main layout component (`frontend/src/components/layout/MainLayout.tsx`) that provides a responsive shell for the application, integrating the desktop sidebar, mobile menu, and React Router's Outlet.
  </objective>
  <read_first>
    frontend/src/App.tsx
    frontend/src/index.css
    frontend/tailwind.config.js
    .planning/phases/03-navigation-page-context/03-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - File `frontend/src/components/layout/MainLayout.tsx` exists.
    - `MainLayout.tsx` contains a responsive layout structure using Tailwind classes (e.g., flex layout).
    - `MainLayout.tsx` renders a `DesktopSidebar` component on larger screens (`md:` breakpoint).
    - `MainLayout.tsx` renders a `MobileMenu` component for smaller screens.
    - `MainLayout.tsx` renders a `main` element that includes a React Router `<Outlet />`.
    - `frontend/src/App.tsx` wraps the application's routes with `MainLayout`.
  </acceptance_criteria>
  <action>
    1. Create `frontend/src/components/layout/MainLayout.tsx` with a basic responsive structure and `Outlet`.
    2. Modify `frontend/src/App.tsx` to use `MainLayout` as the main route wrapper.
  </action>
</task>

<task>
  <objective>
    Implement the `DesktopSidebar` component (`frontend/src/components/layout/DesktopSidebar.tsx`) as a fixed-width navigation element visible on desktop screens, including its custom scrollbar.
  </objective>
  <read_first>
    frontend/src/components/layout/MainLayout.tsx
    frontend/src/index.css
  </read_first>
  <acceptance_criteria>
    - File `frontend/src/components/layout/DesktopSidebar.tsx` exists.
    - `DesktopSidebar.tsx` contains a `nav` element with Tailwind classes for fixed width (`md:w-[108px]`).
    - The sidebar content is scrollable and uses a custom scrollbar contained within the sidebar.
    - Includes primary navigation links, top-aligned.
  </acceptance_criteria>
  <action>
    1. Create `frontend/src/components/layout/DesktopSidebar.tsx`.
    2. Add Tailwind classes for fixed width and styling according to Figma tokens.
    3. Include navigation links mapping from `nav.config.ts`.
  </action>
</task>

<task>
  <objective>
    Develop the `MobileMenu` component (`frontend/src/components/layout/MobileMenu.tsx`) to provide a slide-out navigation experience on mobile, including a frosted glass backdrop and smooth Tailwind transitions.
  </objective>
  <read_first>
    frontend/src/components/layout/MainLayout.tsx
    frontend/src/index.css
  </read_first>
  <acceptance_criteria>
    - File `frontend/src/components/layout/MobileMenu.tsx` exists.
    - Component accepts an `isOpen` prop to control visibility.
    - Applies Tailwind `transition-transform` classes for slide-in/out animations (no Framer Motion).
    - A backdrop element with `backdrop-blur` (e.g. `backdrop-blur-[10px]`) is present when the menu is open.
  </acceptance_criteria>
  <action>
    1. Create `frontend/src/components/layout/MobileMenu.tsx`.
    2. Apply pure Tailwind transition classes for the slide animation.
    3. Add a backdrop with `backdrop-blur` styling.
  </action>
</task>
</plan>

<must_haves>
- Responsive layout with desktop and mobile navigation.
- Pure Tailwind CSS transitions (no Framer Motion).
</must_haves>
