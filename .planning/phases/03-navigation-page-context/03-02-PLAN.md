---
wave: 2
depends_on:
  - 1
files_modified:
  - frontend/src/store/useChatStore.ts
  - frontend/src/utils/chat.ts
  - frontend/src/pages/ChatPage.tsx
autonomous: true
requirements: CHAT-07, LAY-05, GAL-01, GAL-02
---
<plan>
<task>
  <objective>
    Modify `useChatStore.ts` to isolate chat history per page/route, and persist it to `localStorage`.
  </objective>
  <read_first>
    frontend/src/store/useChatStore.ts
    .planning/phases/03-navigation-page-context/03-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `frontend/src/store/useChatStore.ts` imports `persist` from `zustand/middleware`.
    - Chat messages state is keyed by route (e.g. a dictionary/map of `Record<string, Message[]>`).
    - After navigating away and returning to a route, the chat history for that specific route is restored.
    - Data persists across browser refreshes via `localStorage`.
  </acceptance_criteria>
  <action>
    1. Import `persist` and wrap the `create` function in `useChatStore.ts`.
    2. Refactor state from a single `messages: Message[]` array to `sessions: Record<string, Message[]>`.
    3. Update `addMessage` and `clearMessages` to accept and use a `route` parameter.
  </action>
</task>

<task>
  <objective>
    Update the chat submission logic to handle AI-driven route redirection and templated page injection.
  </objective>
  <read_first>
    frontend/src/store/useChatStore.ts
    frontend/src/pages/ChatPage.tsx
    .planning/phases/03-navigation-page-context/03-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - The backend payload dynamically includes a system prompt detailing available routes/pages.
    - `sendMessage` parses the backend response: if it contains a redirection signal, the app uses `useNavigate` to change routes.
    - If it's a standard response, it is appended to the current route's chat history.
    - `ChatPage.tsx` checks if the current route corresponds to a templated page (like Project Gallery) and conditionally renders that rich component *within* the chat feed instead of replacing it.
  </acceptance_criteria>
  <action>
    1. In `useChatStore.ts`, modify the fetch payload to include a system prompt string detailing available routes and the redirection instruction.
    2. Intercept the backend response string: if it matches the redirection format, trigger a route change.
    3. In `ChatPage.tsx`, map specific routes (e.g., `/projects`) to render a placeholder rich component inline within the message list area.
  </action>
</task>
</plan>

<must_haves>
- Chat history persists and is strictly isolated per route.
- Backend redirection logic correctly routes the user dynamically.
- Templated pages render inside the chat feed.
</must_haves>
