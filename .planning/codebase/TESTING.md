# Testing

**Date:** 2026-04-23

## Backend
- **Framework**: `pytest` (`pytest-asyncio` for async tests).
- **Location**: `LightRAG/tests/`.
- Configured in `pyproject.toml` with `asyncio_mode = "auto"`.

## Frontend
- **Framework**: `vitest`, `@testing-library/react`.
- **Scripts**: `npm run test`
